# LVH Release Notes

## [0.7.0-alpha] - 2026-03-28
### ロマン全振り: 「究極の VU メーター」の実装とビジュアライザー基盤の構築
アナログの質感を徹底的に追求した VU メーターを実装し、デスクトップを彩るビジュアライザー設定を追加しました。

#### 追加・変更内容
- **究極の VU メーター (Ultimate VU Meter)**:
    - **物理エミュレーション (`VUPhysicsEngine`)**: 針の「重み」や「空気抵抗」をシミュレート。300ms の立ち上がり時定数と、0dB を超えた際の絶妙なオーバーシュート（揺れ）を再現。
    - **2種類のバックライトテーマ**: 暖かみのある電球色の `Vintage Warm` と、サイバーなオレンジの `Oxygen Neon` を切り替え可能。
    - **独自ウィンドウ構造**: 枠なし（Borderless）かつ、メーター面がクリック透過（Click-Through）する特殊ウィンドウを Win32 WndProc サブクラス化により実現。
    - **フローティング表示**: 常に最前面（Always on Top）で、他のアプリケーションの上で針の振れを楽しめます。
- **Settings (設定画面) の拡張**:
    - **Visualizer タブの新設**: VU メーターのテーマ選択、および全体の透明度（20%〜100%）をリアルタイムに調整可能に。
    - **多言語対応**: Visualizer タブ内の全項目を日本語・英語に対応。
- **オーディオ・エンジン (`GainAndMeterProcessor`) の強化**:
    - **高精度 RMS 計測**: 針の振れを支えるため、信号の二乗平均平方根 (RMS) をリアルタイムに算出するロジックを追加。
- **バージョン・アップデート**:
    - メジャーな新機能追加に伴い、バージョンを `0.7.0-alpha` へ更新。

#### バグ修正
- **透明度変更の即時反映**: JUCE の Layered Window 属性を最適化し、設定変更が即座にウィンドウ外観に反映されるよう修正。
- **ジオメトリの最適化**: 針のピボット位置を調整し、メーター面の視認性とデザインバランスを改善。

## [0.6.5-alpha] - 2026-03-27
### バグ修正: 起動情報表示 & MIDI IN ラベル修正

#### 修正内容
- **起動時バージョン表示**: 起動時にシステムログへバージョン・OS・CPU・RAM情報を出力するよう追加。
- **バージョン番号の修正**: `getApplicationVersion()` が `0.1.0` のまま古い値を返していた問題を修正。
- **MIDI IN ラベルの修正**: MIDI デバイスを切り替えてもツールバーの表示が `No MIDI` のまま更新されなかった問題を修正。起動時および切り替え時に選択中のデバイス名 (`MIDI IN: [デバイス名]`) を正しく反映するよう修正。

---

## [0.6.4-alpha] - 2026-03-27
### ライブのリズムを支える「心臓部」: メトロノーム機能の完全実装
演奏の基盤となる高機能メトロノームを実装し、ミキサー周りの細かな UX 改善とバグ修正を統合しました。

#### 追加・変更内容
- **メトロノーム (Metronome) ウィンドウの新設**:
    - **独立 UI ウィンドウ**: メインツールバーから即座にアクセス可能な、専用のフローティングパネルを実装。
    - **Techno モード (YMO CLICK)**: 2 小節周期でアクセントが「キ・カ・コ」と変化する、ライブ演奏に最適なクリック音を搭載。
    - **タップテンポ機能**: ボタンの連続タップ（8回）により BPM を自動推定・設定。3 秒間のスライディングウィンドウによる精密な計算ロジックを採用。
    - **視覚的インジケーター**: ビートの進行（頭拍/弱拍）に合わせて色が変化する LED ガイドを搭載。
    - **BPM 微調整**: `+/- 10`, `+/- 1` ボタンによる迅速なテンポ変更をサポート。
    - **プロジェクト永続化**: BPM、音量、拍子、クリックタイプ、ウィンドウ位置をプロジェクトファイル (.lvh) に完全に保存。
- **ミキサー・コンソール (Mixer Console) のバグ修正と QoL 向上**:
    - **FX スロット表示の安定化**: マスターストリップの FX スロットが非表示になったり、リサイズが反映されなかったりする問題を修正。
    - **マスター用ラベルの修正**: マスターのエフェクトスロット名称を `LVH-Bridge [FX]:` に統一。
    - **デッドロック（フリーズ）の解消**: FX ブリッジウィンドウを閉じた直後にミキサーを操作した際に発生していた、IPC 通信のデッドロックを修正。
- **ビジュアル・ブラッシュアップ**:
    - **メトロノームアイコンの刷新**: より視認性が高く、リズムを感じさせるクラシックかつ機能的なデザインにアップデート。

---


## [0.6.1-alpha] - 2026-03-26
### ステージ機能の進化: Global Layer と終了処理の安定化
ライブパフォーマンスにおける最強の武器となる「Global Layer (常駐レイヤー)」機能を実装し、設定画面の利便性向上とアプリケーション終了時の堅牢性を大幅に強化しました。

#### 追加・変更内容
- **Global Layer (常駐レイヤー) の導入**:
    - **Slot 0 常駐化**: Stage Set の 1 番目のプロジェクト (Slot 0) を「Global」として扱い、他の曲に切り替えてもマスターエフェクトや特定の音色を鳴らし続けられるように改善。
    - **Global Layer Switch**: 曲の切り替え時に、Global 由来のブリッジや設定を維持したまま、楽器（Local）のみをスマートに入れ替える仕組みを構築。
    - **視覚的フィードバック**: ミキサー上で Global 由来のチャンネルを「金色（ゴールド）」のカラーリングで強調表示し、一目で常駐レイヤーを識別可能に。
- **終了処理の安定化と堅牢性向上**:
    - **ChildProcess 管理の刷新**: Bridge プロセスの終了待機ロジックを強化。X ボタン終了時にフリーズしたり、タスクマネージャーにプロセスが残ったりする問題を完全に解消。
    - **タイムアウト強制終了**: 万が一 Bridge が自発的に終了しない場合でも、3秒のタイムアウト後に確実にクリーンアップを実行。
- **Settings (設定画面) の QoL 向上**:
    - **プラグイン再スキャンボタン**: Settings > Plugin Paths に「Rescan Plugins」ボタンを新設。フォルダ追加後などに即座にスキャンを実行可能に。
    - **UI レイアウト刷新**: マージンや行間の最適化を行い、よりゆったりとしたプロ品質の質感にブラッシュアップ。
    - **完全 i18n 化**: 設定画面内の全てのハードコード文字列を排除し、多言語対応を徹底。
- **バグ修正**:
    - **FX チェーン無音バグの修正**: Instrument スプリットに対して 2 つ目以降のエフェクトを追加した際に、オーディオがミュートされてしまう問題を解決 (Ver 0.4.4-alpha 内容を含む)。

---

## [0.4.3-alpha] - 2026-03-26
### 根本的な安定性とオーディオ品質の向上
MIDI 通信のスレッドモデル刷新、マルチバス VST3 プラグイン対応、およびリアルタイム処理における低レイテンシー化・ノイズ低減を実現しました。

#### 追加・変更内容
- **オーディオ・エンジンの堅牢化**:
    - **マルチバス対応**: RF-Drums などの多数の出力バスを持つドラム音源やサンプラーでの無音問題を解消。
    - **ヒープアロケーション排除**: オーディオスレッドでのバッファ確保を排除し、プロセスの安定化とノイズ耐性を向上。
    - **高品質 MIDI タイムスタンプ**: プロセス間でのタイムスタンプ付与を厳密化し、演奏ジッターを低減。
- **MIDI 通信の改善**:
    - **非同期的ディスパッチ**: Bridge への MIDI 送信を専用スレッドに分離し、UI フリーズやタイミング遅延を完全に防止。
    - **MIDI 二重送信の修正**: 特定の環境でハードウェア MIDI が二重に送信されていたバグを根本解決。
- **リリースビルドの安全性向上**:
    - **Atomic 状態管理**: リリースビルドの最適化により MIDI が届かなくなる場合がある問題を、アトミック変数によるスレッドセーフ化で修正。
- **IPC 診断の最適化**:
    - 冗長な MIDI 受信ログを整理し、高密度な演奏時でもログ書き込みがパフォーマンスを阻害しないように調整。

---

## [0.4.2-alpha] - 2026-03-25
### ライブ演奏機能の強化: Stage Mode & MIDI 外部制御の統合
ライブパフォーマンスにおける操作性と信頼性を飛躍的に向上させる「Stage Performance Mode」と、外部 MIDI コントローラとの高度な連携機能（MIDI Learn）を実装しました。

#### 追加・変更内容
- **Stage Performance Mode (セットリスト管理)**:
    - 複数のプロジェクト (.lvh) を 1 つのセットとして管理し、一発で切り替え可能に。
    - **Dirty フラグ監視**: 未保存の変更がある場合の確認ダイアログ表示とタイトルバーへの `*` 表示。
    - **誤操作防止**: 「選択 (Selection)」と「実行 (Active)」を分離し、ライブ中の誤クリックを防止。
    - **キーボード/MIDI 操作**: 矢印キーや MIDI フットコントローラによる曲選択・ロードに対応。
- **MIDI 外部制御 & MIDI Learn**:
    - **MIDI Learn**: ミキサーのフェーダー、パン、ミュート、ソロを右クリックして MIDI CC を即座にマッピング可能。
    - **マスターリモート**: 全体のボリュームを外部コントローラから制御可能。
    - **設定保存**: 全てのマッピング情報はアプリケーション設定として自動保存・復元されます。
- **多言語対応 (i18n)**:
    - **言語切替**: 設定画面から日本語・英語を動的に切り替え可能（再起動不要）。
    - **文字化け修正**: グローバルなフォント・マッピングを導入し、Windows 環境での日本語表示を安定化。
- **UI/UX の洗練**:
    - **ウィンドウのピン留め**: Stage および Mixer ウィンドウをデスクトップ最前面に固定可能に。
    - **コア・ツールバー統合**: メイン画面から Stage ウィンドウを直接開閉できるボタンを追加。
    - **安全性の向上**: ロードエラー通知の強化と、保存ダイアログの初期ファイル名表示。

---

## [0.2.1-alpha] - 2026-03-23
### 操作性の改善: ミキサー主導のプラグイン追加ワークフロー
ミキサーから直接エフェクトを追加できるフローを導入し、不要なメニューを整理して「音作りのハブとしてのミキサー」を強化しました。

#### 追加・変更内容
- **ミキサーからの直接 FX 追加**:
    - 各チャンネルおよびマスターの空き FX スロットに `+` アイコンを表示。
    - `+` をクリックすることでスキャン済みプラグイン一覧を表示し、即座にエフェクトとして追加可能に。
- **自動ロール判定の導入**:
    - プラグインリストからロードする際、`isInstrument` フラグを元に Instrument プロセスか Effect プロセスかを自動判別するように改善。
- **UI/UX の整理**:
    - ロゴメニューからファイル選択方式の個別ブリッジ起動メニューを削除し、スキャン済みリストへのアクセスに統合。
    - ロゴメニューの名称を「Select Instruments」から、エフェクトも含む「Select Plugins」へ変更。
    - マスターストリップの FX スロットを常に 4 つ表示するように固定し、視認性を向上。

---

## [0.2.0-alpha] - 2026-03-23
### アーキテクチャ刷新: God Class 解体と安定性・速度の向上
大規模なリファクタリングを実施し、`Main.cpp` に集中していた機能を 4 つの独立した Manager クラスに分散。将来の拡張（Stage Performance Mode 等）に向けた強固な基盤を構築しました。

#### 追加・変更内容
- **大規模アーキテクチャ・リファクタリング**:
    - **MidiRoutingManager**: MIDI ルーティングとオクターブシフトの専門管理クラスを新設。
    - **ProjectSerializer**: XML のパース、プロジェクト保存・復元、保留状態の管理を独立化。
    - **BridgeManager**: ブリッジプロセスのライフサイクル（起動、切断、リスト管理）の集約。
    - **UIManager**: ウィンドウ管理、メニュー構築、音量制御などを抽出し UI ロジックをスリム化。
    - `Main.cpp` のコード量を約 1160 行から約 370 行まで削減。純粋なオーケストレーターとして再定義。
- **パフォーマンス最適化**:
    - 新しい「クロージャ注入パターン」により、ブリッジ接続時の保留データ検索を O(1) に改善。
    - リリースビルドにおいて、プラグインのロード時間が体感できるほど高速化。
- **FX バイパス機能**:
    - ミキサー画面から各ブリッジの FX を個別にバイパス可能に（プロセッサー側でのスルー処理を実装）。
    - バイパス状態はプロジェクトファイル (.lvh) に保存・復元されます。
- **MIDI 入力設定の永続化**:
    - 選択した MIDI 入力デバイスの情報をプロジェクトごとに保存・自動復旧するように改善。
- **安定性とスレッド安全性の向上**:
    - MIDI ディスパッチ処理に atomic 変数を採用し、リアルタイム演算中の設定変更に対する堅牢性を強化。
    - マネージャー間の循環参照を完全に排除し、安全な初期化・破棄順序を保証。

---

## [0.1.0-alpha] - 2026-03-19
### 大幅アップデート: ステージ版への移行・基盤構築
LIGHT-VST-HOST (Light版) の設計を継承しつつ、マルチプロセス・サンドボックス構造を採用した ステージ版の初期アルファ版 0.1.0 を公開します。

#### 追加・変更内容
- **マルチプロセス・アーキテクチャへの刷新**:
    - オーディオエンジン (Core) と VST3 ブリッジ (Bridge) を分離。
    - 各プラグインは独立したプロセスとして動作し、クラッシュ時の安定性が向上。
- **IPC (Inter-Process Communication) 実装**:
    - 共有メモリおよび Named Pipes による Core-Bridge 間の超低レイテンシー通信。
    - 音声データ (Stereo) の双方向同期、MIDI メッセージの転送。
- **プロジェクト保存・読込機能 (.lvh)**:
    - 現在のプロジェクト状態（プラグイン、音色、ウィンドウ位置）を XML 形式で保存・復元可能に。
    - プラグイン内部状態 (Base64) の永続化に対応。
- **ウィンドウ位置記憶**:
    - 各 Bridge のウィンドウ座標をプロジェクトごとに記憶し、再開時に自動復元。
- **UI/UX の刷新**:
    - メインツールバーにミュートボタン、ボリュームスライダー、CPU/MIDI モニター、システムログを追加。
    - PCキーボードのオクターブシフト機能（ `[` / `]` キー）の実装。
    - Middle C 表記を VST3 規格（C4=MIDI 72）に統一し、演奏性を改善。
- **設定（Preferences）の強化**:
    - プラグイン検索パスの任意追加・削除、および自動再スキャン機能。
- **MIDI ルーティングの強化**:
    - 「全 Bridge 一斉送信」と「個別 Bridge 送信」を指定可能。

---

## [0.0.9] - 2026-03-16
### 追加
- 正式なベータ版としてリリース (旧 Light 版)
- デスクトップ用アイコン（LVH_desktop_icon.ico）を設定
- UI/UXの改善（これまでの修正内容を含む）