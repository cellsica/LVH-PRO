# LVH (Everybody's Version) 仕様書 (Ver 0.6.1-alpha)

## 1. プロジェクト概要
Live-ready VST Host (LVH) の設計を継承しつつ、マルチプロセス・サンドボックス構造を採用することで「絶対的な安定性」と「自由なレイアウト」を実現したプロフェッショナル向け VST ホスト。

### ターゲット
- プラットフォーム: Windows 10/11 (64bit)
- 対応規格: VST3 (SDK 3.7.x 準拠)
- 開発言語: C++17 / JUCE Framework

## 2. Pro版 核心機能
### 2.1 マルチプロセス・サンドボックス
- **Commander (Core Process)**: オーディオデバイス、MIDI 入力、全体の同期、プロジェクト保存を管理。
- **Bridge (Plugin Process)**: プラグインごとに独立したプロセスとして起動。1つのプラグインのクラッシュがシステム全体に影響しない。
    - **Robustness**:
        - **マルチバス対応**: ドラム音源やマルチアウト音源（VST3）のバスレイアウトを自動検知し、Bridge 側の Stereo 入出力と整合性を保つ「強制ステレオ・レイアウト」機能を搭載。
        - **スレッド安全性**: ブリッジ状態管理に `std::atomic` を採用し、リリースビルドにおけるメモリアクセス最適化による誤作動を防止。
        - **Heartbeat Watchdog**: Bridge プロセスの死活監視を `juce::Timer` ベースのウォッチドッグで実装。5 秒間ハートビートが途絶した場合、JUCE 内部の `safeAction` 機構をバイパスして `onDisconnected` を直接発火し、UI・オーディオグラフを正確に更新する。
- **IPC (Inter-Process Communication)**:
    - **Named Pipe**: 制御メッセージ（Midi, Config, State, WindowPos）の送受信。
    - **Shared Memory**: オーディオデータのゼロコピー転送（2ch / 最大 4096 samples）。
    - **Named Events**: Windows イベントによるオーディオループの厳密な同期。
    - **Async MIDI Dispatch**: Core から Bridge への MIDI 送信を専用スレッドで非同期化し、UI および演奏のブロッキングを完全に排除。

#### 2.1.1 リアルタイム処理の最適化
- **No-Heap Processing**: オーディオスレッドでの動的なヒープメモリ確保（AudioBuffer の生成等）を完全に排除。バッファの再利用により、サンプラー音源等でのノイズ・音切れを最小限に抑制。
- **MIDI Timestamping**: 全プロセス間での高精度な MIDI タイムスタンプの同期により、リズムの揺らぎ（ジッター）を低減。

### 2.2 独立ウィンドウ管理 (Window Orbit)
- 各プラグインは個別の OS ウィンドウとして表示され、画面上のどこにでも配置可能。
- **ウィンドウ位置記憶**: プロジェクト保存時に各 Bridge の座標(X, Y, W, H)を記録し、再開時に復元。

### 2.3 プロジェクト管理 (.lvh)
- **状態の永続化**: 
    - ロードされている全プラグイン、その音色データ（State Information）、MIDI ルーティング、トランスポーズ、オクターブ設定を XML 形式で保存。
    - **非同期セーブ**: 各プロセスから音色データを収集し、タイムアウト管理付きで安全に保存する仕組み。

### 2.4 ミキサー・コンソール (Mixer Console)
- **独立ウィンドウ表示**: メインウィンドウとは別に立ち上がる、フローティング形式のミキサー。
- **動的チャンネルストリップ**: ロードされている Bridge (Instrument) を自動検出し、リアルタイムでストリップを生成・破棄。
- **ミキサー操作**:
    - **フェーダー・パン**: 各チャンネルの音量（0.0〜1.5）およびパン（L1.0〜R1.0）を制御。
    - **ミュート・ソロ**: DAW 流のミュート、および複数選択可能なソロ機能を搭載。
    - **リアルタイム・メーター**: 30fps で駆動するセグメント LED アニメーション。
- **マスター同期**:
    - Core ツールバーの簡易音量スライダーとミキサーの MASTER フェーダーが完全連動。
    - どちらを操作しても双方向で値が同期し、プロジェクト保存時には常に最新のマスター音量が記録される。
- **Global チャンネルの視覚的識別**:
    - Global Layer (Stage Set Slot 0) 由来のチャンネルストリップは、金色のトップバーとボーダーで装飾され、曲ごとのローカルチャンネルと一目で区別できる。
- **最前面固定 (Pin)**:
    - 独立ウィンドウをデスクトップの最前面に固定可能。ライブ中のウィンドウ紛失を防止。
    - 設定状態は `ApplicationProperties` により永続化され、再起動後も維持される。

### 2.5 Stage Performance Mode (ライブ演奏モード)
- **Stage Set (.stg)**: 
    - 複数のプロジェクト (.lvh) を 1 つのセットとして管理する JSON 形式の定義ファイル。
    - **セット名 (setName)**、プロジェクトファイルのフルパス、表示用のエイリアス (別名)、および再生順序を記録。
- **セットリスト・マネージャー**:
    - 専用の UI ウィンドウにて、セットリストの追加・削除・並び替えを実行可能。
    - **Dirty フラグ監視**: 
        - 未保存の変更がある状態で新規作成・ロードを行う際の確認ダイアログ表示。
        - 変更がある場合はウィンドウタイトルに `*` を表示して保存状態を明示。
    - **「選択 (Selection)」と「実行 (Active)」の分離**: 
        - リスト項目をクリックすると「選択状態（青枠）」になり、LOAD ボタンを押すと「アクティブ状態（深緑背景）」となってプロジェクトがロードされる。ライブ中の誤爆を防止。
    - **キーボード操作**: `↑` / `↓` キーによるリスト選択の移動、`Enter` キーによるプロジェクト・ロードをサポート。
    - **エラー通知**: `.stg` 内で指定されたプロジェクトファイルが見つからない場合、詳細なエラーメッセージを表示。
    - **最前面固定 (Pin)**:
        - 演奏中に他のウィンドウに隠れないよう、最前面に固定する「ピン」機能を搭載。
        - ピンの状態（ON/OFF）はアプリケーションを跨いで保持される。
    - **ドラッグ＆ドロップ**: リスト内での直感的な並び替え（挿入位置の視覚的ガイド付き）。
    - **右クリックメニュー**: 項目の名前変更(Rename Alias)や削除(Delete)をサポート。
    - **ウィンドウ永続化**: ウィンドウの表示状態と表示座標(X, Y, W, H)をアプリケーション終了時に保存し、次回起動時に復元。
    - **Global Layer (常駐レイヤー)**:
        - **Slot 0 特権化**: Stage Set の Index 0 に登録されたプロジェクトを「Global プロジェクト」として扱い、そのブリッジに `isGlobal=true` フラグを付与する。
        - **Global Layer Switch モード (Slot 1+)**: Slot 1 以降をクリックした場合、「楽器のみの入れ替え」モードで動作する。
            - **設定継承**: ウィンドウ位置・サイズ、マスターボリューム、MIDI ルーティングは Slot 0 の設定をそのまま引き継ぎ、新しいプロジェクトの Settings / MidiRouting ブロックは適用しない。
            - **Master FX の永続化**: Slot 0 のマスター FX ブリッジ（`Role=Effect` かつ `fxParentPath` が空）は切り替え後も生存し続ける。Slot 1+ の .lvh に記述された Master FX エントリはスキャン時にスキップされる。
            - **楽器の差し替え**: Slot 0 の Instrument ブリッジ（および per-channel FX）はアンロードされ、Slot 1+ の Instrument ブリッジが新たに起動する。
        - **音声グラフ順序**: `rebuildBridgeGraph()` 内でローカルブリッジ（楽器→チャンネル FX）を先に処理し、グローバルブリッジ（Master FX）を末尾に配置することで、正しいシグナルフローを保証する。
        - **動的 Global Layer**: Global Layer はセット使用時のみ有効。直接 .lvh を開いた場合は通常ロード動作となる。Slot 0 の順番が変われば以降の動作も追随する。
        - **視覚的識別**: ミキサー上で Global 由来のチャンネルストリップには金色のトップバーとボーダーを表示し、ローカルチャンネルと視覚的に区別する。
    - 演奏中、楽器を持ち替えるように環境を丸ごとスイッチするためのライブ特化機能。

## 3. UI/UX 機能

- **メインツールバー**: 
    - ボリュームスライダー、ミュート、CPU 負荷表示、MIDI モニター、システムログ。
    - **Stage Set ボタン**: ライブ演奏用セットリスト画面を一発で開閉可能な独立ボタン。
- **多言語対応 (i18n)**:
    - **LanguageManager**: シングルトンによる全 UI 文字列の統合管理。
    - **動的切替**: 設定画面からの変更が、再起動なしで全ウィンドウに即座に反映される。
    - **標準サポート**: 日本語および英語をフルサポート。
- **日本語対応 (Look & Feel)**:
    - グローバルなフォント・マッピングにより、アプリケーション全域での日本語表示をサポート（Yu Gothic UI / MS Gothic 適用）。
- **オクターブ・トランスポーズ**:
    - `[` / `]` キーによるオクターブシフト（±3オクターブ）、トランスポーズ設定。
    - Middle C 表記を VST3/Steinberg 規格（MIDI 72 = C4）に統一。
- **MIDI リモート制御**:
    - **マスターボリューム CC**: 特定の MIDI CC を割り当て、外部コントローラから全体の音量を操作可能。
    - **ステージリモート**: Program Change または Control Change によるセットリストの項目選択（Prev/Next）および LOAD 実行をサポート。
    - **設定**: 設定画面（MIDI Settings）にて、受信チャンネルや各アクションの CC 番号をカスタマイズ可能。
- **MIDI ルーティング**:
    - 全ての Bridge への一斉送信、または特定の Bridge への個別ルーティングをメニューから切替可能。
- **設定画面 (Preferences)**:
    - 4つのカテゴリ（General, Audio, MIDI, Plugin Paths）に整理された設定 UI。
    - **言語設定**: アプリケーションの使用言語を選択可能。
    - オーディオ/MIDI デバイス設定。
    - **カスタム・プラグインスキャンパス**: ユーザーが任意の VST3 フォルダを追加可能。追加・削除時に自動的に再スキャンが実行される。

## 4. 技術スタック詳細
- **言語**: C++17
- **GUI / Audio**: JUCE 7.0.x
- **Build System**: CMake / Visual Studio 2022
- **IPC 原理**: 
    - Control: `juce::InterprocessConnection` (Named Pipe)
    - Audio: `juce::SharedResourcePointer`, `CreateFileMapping`, `CreateEvent`

## 5. ロードマップ (今後の予定)
- 独立ウィンドウの「親プロセス内への吸着（ドッキング）」シミュレーション。
- 各 Bridge からの個別オーディオ出力（マルチアウト）対応。
- オーディオ・エンジン（Graph）の視覚化エディタ。