# 【中間資料】Pro版拡張を見据えたアーキテクチャ再設計・仕様ドラフト

## 1. 背景と目的
現在、`LIGHT-VST-HOST (LVH)` は単一のVSTプラグインをロードして演奏する「シングルスロット」の構成です。Pro版において「複数プラグインの同時使用（マルチスロット）」を実現するため、既存のコードからエンジン部を分離し、将来の拡張に耐えうるインターフェースを定義します。

## 2. 検討すべき主要コンポーネント

### A. PluginSlot (プラグイン・スロット)
プラグイン1つ分を独立した単位として扱います。
- **管理データ**:
    - `AudioPluginInstance`: プラグイン本体（VST3の実体）。
    - `AudioProcessorGraph::NodeID`: グラフ上でのこのスロットの住所。
    - `MemoryBlock`: プラグイン固有の現在のパラメータ状態（プリセット情報）。
- **インターフェース案**:
    - `load(const PluginDescription&)`: プラグインを実体化。
    - `unload()`: プラグインを解放し、スロットを空にする。
    - `bypass(bool)`: 音声処理をバイパス。
    - `getEditor()`: UI表示用のエディタを生成。
    - `getLatencyInSamples()`: プラグインが発生させる遅延量を取得（ADC用）。

### B. AudioEngine (オーディオ・エンジン)
複数のスロットを束ね、`AudioProcessorGraph` を管理します。
- **管理データ**:
    - `OwnedArray<PluginSlot>`: 複数のスロットを保持。
    - `AudioProcessorGraph`: ノード間の信号接続（ルーティング）を制御。
- **インターフェース案**:
    - `addSlot()`: 新しいプラグインスロットを作成。
    - `removeSlot(int index)`: 指定したスロットを削除。
    - `rebuildGraph()`: スロット間の接続順序（シリアル/パラレル）を再構築。
    - `allNotesOff()`: 全スロットに対して MIDI Panic を送信。
    - `calculateTotalLatency()`: システム全体の遅延を計算。

### C. HostController (ホスト・コントローラー / Pro ダッシュボード連携)
UI層、および Pro版の親エンジン（ダッシュボード）との橋渡しを担います。
- エンジンからのステータス通知（ロード完了、エラーなど）をUIに伝える。
- 外部コア/プロセスとの通信インターフェースを定義。

## 3. 解決すべき技術的課題（もみどころ）と設計指針

### 3.1. 低レイテンシー・高音質設計 (なべ・茜提言)
- **ゼロコピーの徹底**:
  - 同一プロセス内では `AudioBuffer` のポインタ渡しを行い、信号処理のオーバーヘッドをゼロに近づける。
- **ADC (Automatic Delay Compensation) の実装**:
  - 各スロットが報告するレイテンシーを集計し、パラレル接続時の位相ズレを自動補正。茜さんの「信頼の証」を担保する。
- **グラフの平坦化 (Graph Flattening)**:
  - 複雑なコア間接続を、最終的に単一の計算グラフに展開してオーディオコールバックで一括処理する。

### 3.2. 耐障害性と自由度の両立
- **プロセス分離の検討**:
  - 茜さんの提言通り、特定のプラグインが落ちてもシステム全体を道連れにしないため、不安定なプラグインを「サンドボックス（別プロセス）」で隔離するオプションを検討。
- **モジュラーな設定の使い回し**:
  - コア単位での状態保存（Serialization）を強化し、「インストゥルメント」や「エフェクトラック」を別プロジェクトへサクッと持ち出せるようにする。

## 4. 次のアクション
1. 本ドキュメントの内容に基づいて、なべさんと茜さんで「やりたいこと」の整合性を最終確認。
2. 仕様が固まり次第、`Main.cpp` からのクラス抽出・分離実装を開始（まずは `PluginSlot` から）。
