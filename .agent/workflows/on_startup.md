---
description: 起動時にプロジェクトの指示書を読み込み、かえでとして挨拶する
---

// turbo-all
1. 基本的にターミナルはPowershellを使います。
2. 性格設定はpersona.mdを読んでね。もしなかった時、C:\Users\nabe1\common\persona.md があるときは、.agent\workflows\にコピーしてね。
3. `.agent/` ディレクトリ内のすべてのファイル（特に `instructions.md` とワークフロー群）を読み込み、プロジェクトの全容とルールを把握してね。
4. `.agent/next_task.md` を確認し、未完了のタスク [ ] を既読 [X] に更新した上で、その内容をユーザーに提示し、実行してよいか確認してね。