# Live-ready VST Host Pro (LVH-Pro) アーキテクチャ設計・移行計画書

## 1. コンセプト：ハイパー・モジュラー・コンソール
Pro版は、単なる「多機能版」ではなく、ユーザーが自身でコンソール環境を再構築できる**「完全自由配置型ホスト」**を目指します。

### 核心となる思想
- **真の独立性**: すべてのUI要素（メーター、プラグイン、エディタ）がOSレベルで独立したウィンドウとして振る舞い、配置場所を縛らない。
- **究極の堅牢性**: プラグイン1つのクラッシュがシステム全体を止めない。
- **プロフェッショナル・パフォーマンス**: 複数プロセス間でも、共有メモリを用いた超高速通信により、リアルタイム性能を犠牲にしない。

---

## 2. 技術的アーキテクチャ

### A. プロセス外ホスト（プラグイン・サンドボックス）
- **Core Process (Commander)**:
    - オーディオデバイスの管理、MIDI入力、メインエンジンの制御。
    - 各子プロセスとの通信を統括。
- **Plugin Bridge Process (Child)**:
    - プラグインごとに1つのプロセス（またはグループ単位）を生成。
    - 64bit VST2/VST3をロードし、自身のウィンドウでGUIを表示。
    - メモリ空間を独立させることで、クラッシュ時の「共倒れ」を完全に回避。

### B. 超高速 IPC (Inter-Process Communication)
- **Shared Memory (共有メモリ)**:
    - 音声データ（バッファ）の転送には、コピーコストを最小限に抑える共有メモリ方式を採用。
- **Circular Buffer (環状バッファ)**:
    - コアと子プロセスの間で、ロックフリーな環状バッファを用いて低遅延で同期。

### C. 独立ウィンドウ・管理システム
- **Window Orbit**:
    - 全ウィンドウの座標情報を一元管理し、プロファイルとして保存・読み込み可能にする機能。
- **Docking Simulation**:
    - 物理的には独立したウィンドウだが、吸着（スナップ）機能により一体感のある操作性を提供。

---

## 3. Light版からの移行・開発プロセス

このプロジェクトでは、なべ（ユーザー）、かえで（Antigravity）、しずく（Claude Code）が密に連携して移行を進めます。

### ステップ1：環境構築（担当：かえで）
- **リポジトリ作成**: Light版をベースにPro版の初期リポジトリ（またはフォーク）を作成。
- **プロジェクト初期化**: Pro版開発用の `.agent` 設定ファイルを構築。
- **共有インターフェースの抽出**: Light版と将来のPro版で共通して使うオーディオ/MIDIロジックをライブラリ化（Header-only等）の検討。

### ステップ2：大規模リファクタリング（担当：しずく）
- **シングルプロセスからマルチプロセスへの解体**:
    - `AudioEngine` を「管理側」と「実行側（Bridge）」に分離。
    - ウィンドウ表示ロジックを、トップレベルウィンドウとして独立させるためのベースクラス作成。
- **IPC基盤の実装**: JUCEを用いたプロセス間通信プロトコルの構築。

### ステップ3：継続的同期
- **機能バックポート**:
    - Light版で行ったバグ修正や小規模な機能改善を、差分パッチまたは手作業でPro版に常時反映。
    - 「Light版の完成度が、常にPro版の基礎となる」状態を維持。

---

## 4. 最初のアクションアイテム
1. **設計詳細の確定**: 共有メモリのデータ構造（バッファサイズ、同期フラグ）の策定。
2. **Bridgeのエントリポイント作成**: プラグイン1つをロードして表示することに特化した、最小構成の `Bridge.exe` プロトタイプ開発。
3. **リファクタリング用指示書作成**: しずくがスムーズに作業できるよう、MainComponent周辺の解体手順を詳細化。

---
> [!NOTE]
> このドキュメントは、なべさんの「自由なレイアウト」と「堅牢な動作」を両立させるためのマイルストーンです。内容の修正や追加のアイデアがあれば、いつでも言ってね！ by かえで
